float dp09Hash(vec2 p){return fract(sin(dot(p+uSeed,vec2(173.3,269.5)))*43758.545);}
vec4 dp09Read(vec2 uv){return texture(iChannel0,clamp(uv,0.0,1.0));}
void mainBuffer(out vec4 state,in vec2 fragCoord){vec2 uv=fragCoord/iResolution.xy;vec2 px=1.0/iChannelResolution[0].xy;vec4 c=dp09Read(uv);if(iFrame==0){float stroke=step(abs(uv.y-.5),.035)*step(.12,uv.x)*step(uv.x,.88);state=vec4(stroke,.7,0.,1.);return;}vec4 n=dp09Read(uv+vec2(0.,px.y)),s=dp09Read(uv-vec2(0.,px.y)),e=dp09Read(uv+vec2(px.x,0.)),w=dp09Read(uv-vec2(px.x,0.));vec4 avg=(n+s+e+w)*.25;vec4 lap=n+s+e+w-4.*c;vec2 flow=vec2(.8+.2*sin(uv.y*13.),.18*sin(uv.x*17.+float(iFrame)*.04));vec4 adv=dp09Read(uv-flow*px*2.);float brush=step(abs(uv.y-.5-.16*sin(float(iFrame)*.03)),.018)*step(uv.x,.18);state=vec4(max(adv.r*.992,brush),mix(c.g,avg.g,.06),abs(c.r-adv.r),1.);}
void mainImage(out vec4 fragColor,in vec2 fragCoord){vec2 uv=fragCoord/iResolution.xy;vec4 q=texture(iChannel0,uv);float motif=smoothstep(.05,.45,q.r+q.b*.9);float contour=smoothstep(.065,.008,abs(fract((q.r+q.b)*8.)-.5));float pulse=.5+.5*sin(iTime*.73+uv.x*8.-uv.y*6.);vec3 col=mix(uColBg,uColDim,.12+.3*q.a);col=mix(col,uColPaper,clamp(q.r*.72+motif*.22,0.,1.));col=mix(col,uColSignal,clamp(q.g*.65+contour*.25,0.,1.));col+=uColAccent*(q.b*.35+pulse*.04*motif);fragColor=vec4(col,1.);}
